#The code defines a custom app configuration class for a Django app called `drugprediction`.

from django apps import AppConfig

class DrugpredictionConfig(AppConfig)
    
    name = 'drugprediction'
    
